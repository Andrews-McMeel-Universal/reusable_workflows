name: Docker Build CI

on:
  workflow_call:
    inputs:
      dockerContainerPort:
        required: true
        type: string
        description: 'Docker Container Port'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
  # Cancel an in-progress deploy if a newer one is pending

jobs:
  test-in-ci:
    name: Build Docker App
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Docker Build
        uses: docker/build-push-action@v4
        with:
          tags: docker-ci:latest
          push: false
          load: true

      - name: Run yarn test in container
        uses: addnab/docker-run-action@v3
        with:
          image: docker-ci:latest
          run: sleep 30 && curl http://localhost:${{ inputs.dockerContainerPort }}

