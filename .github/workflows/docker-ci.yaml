name: Docker Application CI

on:
  workflow_call:
    inputs:
      environment:
        required: false
        description: Environment to run CI checks in
        type: string
        default: ${{ vars.APPLICATION_CI_ENVIRONMENT || 'development' }}
      healthCheckPath:
        required: false
        description: Path to health check endpoint
        type: string
        default: ${{ vars.APPLICATION_CI_HEALTHCHECK_PATH || '/' }}
      dockerFilePath:
        required: false
        type: string
        description: "Relative path to Dockerfile."
        default: ${{ vars.DOCKER_FILE_PATH || '.' }}
      githubRunner:
        required: false
        type: string
        description: "The type of runner to use"
        default: ${{ vars.CUSTOM_GITHUB_RUNNER || 'ubuntu-latest' }}
    secrets:
      azureCredentials:
        required: false

jobs:
  build:
    name: Build app
    if: ${{ github.actor != 'dependabot[bot]' }}
    runs-on: ${{ inputs.githubRunner }}
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0

      - name: Set up QEMU
        uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c

      - name: Check if azureCredentials is set
        run: |
          if [ -z "${{ secrets.azureCredentials }}" ]; then
            echo "Azure Credentials not set. Skipping Azure Key Vault retrieval."
            echo "AZURE_CREDENTIALS_SET=false" >> $GITHUB_ENV
          fi

      - name: Generate .env file from Azure Key Vaults
        if: ${{ env.AZURE_CREDENTIALS_SET != 'false' }}
        id: get-envs
        uses: Andrews-McMeel-Universal/get-envs@v1
        with:
          azurecredentials: ${{ secrets.azureCredentials }}
          environment: ${{ inputs.environment }}
          contentTypes: BuildArg Env
          buildArgPredicate: ";"
      
      - name: Insert new line after build arguments
        id: insert-new-line
        run: |
          buildArguments=$(echo "${{ steps.get-envs.outputs.buildArguments }}" | sed 's/^ ; //g' | sed 's/; /\n/g')
          echo 'buildArguments<<EOF' >> $GITHUB_ENV
          echo "$buildArguments" >> $GITHUB_ENV
          echo 'EOF' >> $GITHUB_ENV

      - name: Docker Build
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a
        with:
          context: ${{ inputs.dockerFilePath }}
          tags: docker-ci:latest
          push: false
          load: true
          build-args: |
            ${{ env.buildArguments }}

      - name: Get Docker variables from image
        run: |
          dockerContainerPort=$(docker image inspect docker-ci:latest -f "{{json .Config.ExposedPorts }}" | tr -d '{}":/tcp')
          dockerCmd=$(docker image inspect docker-ci:latest -f "{{json .Config.Cmd }}" | tr -d '][' | tr "," " ")

          echo "dockerContainerPort=$dockerContainerPort" >> $GITHUB_ENV
          echo "dockerCmd=$dockerCmd" >> $GITHUB_ENV

      - name: Run tests in Docker Container
        shell: sh
        env:
          DOCKER_CONTAINER_PORT: ${{ env.dockerContainerPort }}
          HEALTH_CHECK_PATH: ${{ inputs.healthCheckPath }}
        run: |
          docker run --rm \
            -e DOCKER_CONTAINER_PORT="$DOCKER_CONTAINER_PORT" \
            -e HEALTH_CHECK_PATH="$HEALTH_CHECK_PATH" \
            -p "${DOCKER_CONTAINER_PORT}:${DOCKER_CONTAINER_PORT}" \
            docker-ci:latest \
            sh -lc '
              ${{ env.dockerCmd }} &
              PID=$!
              sleep 15
              URL="http://localhost:${DOCKER_CONTAINER_PORT}${HEALTH_CHECK_PATH}"
              if kill -0 "$PID" 2>/dev/null
                then
                wget -O - -q "$URL" || curl -fsS "$URL"
              else
                exit 1
              fi
            '
