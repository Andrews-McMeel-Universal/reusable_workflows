apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "charts.name" . }}-admin
  labels:
    {{- include "charts.labels" . | nindent 4 }}
{{- with .Values.adminingress.annotations }}
  annotations:
    {{- toYaml . | nindent 8 }}
{{- end }}
spec:
  tls:
  - hosts:
    - {{ .Values.ingress.host }}
    secretName: {{ .Values.ingress.tlssecret }}
  rules:
  - host: {{ .Values.adminingress.host }}
    http:
      paths:
      - path: /admin
        pathType: Prefix
        backend:
          service:
            name: {{ include "charts.name" . }}
            port:
              number: 80
  